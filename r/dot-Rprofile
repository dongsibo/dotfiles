#
# ~/.Rprofile
#

# This is called after everything else in R_PROFILE_USER
.First <- function() {
    library(colorout)                               # colourize R output
    options(repos = "https://cloud.r-project.org")  # use 0-Cloud CRAN mirror
}

# vim: set filetype=r :
