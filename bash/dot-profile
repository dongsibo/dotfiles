#
# ~/.profile
#

[ -n "$BASH_VERSION" ] && [ -r "$HOME/.bashrc" ] && . "$HOME/.bashrc"

[ -d "$HOME/bin" ] && PATH="$HOME/bin:$PATH"
[ -d "$HOME/.local/bin" ] && PATH="$HOME/.local/bin:$PATH"

export PATH

export EDITOR=vim
export VISUAL=vim

# Universal options for less(1)
export LESS="\
--quit-if-one-screen \
--ignore-case \
--quit-on-intr \
--RAW-CONTROL-CHARS"

# Systemd has its own set of options that it passes to less. Options from LESS
# are not applied. I like the defaults used for systemctl(1) and
# journalctl(1), but I want to also add more options like case-insensitive
# search. Setting SYSTEMD_LESS overrides the default, so all desired options
# must be defined here.
export SYSTEMD_LESS="\
--chop-long-lines \
--no-init \
--LONG-PROMPT \
$LESS"

export FZF_DEFAULT_OPTS='--color=bg+:237'

# Enable Wayland for Firefox
[ "$XDG_SESSION_TYPE" = wayland ] && export MOZ_ENABLE_WAYLAND=1

# Disable GStreamer VA-API driver whitelist (to use iHD from
# intel-media-driver). This is for gstreamer-vaapi 1.16. 1.18 will apparently
# have iHD from intel-media-driver included in the whitelist, so I can remove
# this line once that time comes. To see if this works, I should check that
# there are more than 0 features found when running `gst-inspect-1.0 vaapi`.
# See https://blogs.igalia.com/vjaquez/2020/01/08/gstreamer-vaapi-1-16-and-libva-2-6-in-debian/
# for more details.
export GST_VAAPI_ALL_DRIVERS=1

# vim: set filetype=sh :
